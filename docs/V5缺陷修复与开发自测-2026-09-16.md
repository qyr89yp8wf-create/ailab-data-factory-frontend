# V5 缺陷修复与开发自测

日期：2026-09-16。环境：http://127.0.0.1:8769/。V4 未修改。

## 修复结果

| 问题 | 修复 | 保留的行为 |
| --- | --- | --- |
| F01 图像扩增错误模型 | 文档图像的首组、新增组和主扩增模型统一默认 Doubao-Seedream-4.0；下拉只展示现有图像生成模型；旧配置不静默改写，提交时拒绝不适用模型 | 自定义多组、目标标签、指令、参数 JSON、增删组与其它模态原选项保留 |
| F02 默认100超过输入98 | 选择或切换增强来源时立即将数量约束在来源范围，保留范围内手动值；提交检查正整数及上限；零样本禁止提交 | 定向扩增不受来源样本数量上限影响 |
| F03 发布模板显示草稿 | 按真实 draft_id 区分草稿与发布记录，分别展示身份、ID及发布版本；脏草稿提示未保存 | 五步导航与只读权限保留 |
| F04 评分口径冲突 | 同步旧验收清单；页面说明整体分数不能替代逐规则结果、不代表总体合格结论 | 现有等权平均算法、导出字段、六状态和逐规则结果不变 |
| B01 下载难以观察 | 报告及版本下载挂载临时链接后触发，移除临时链接；三个下载入口展示“已开始下载”反馈 | 仍导出现有 JSON / ZIP 内容，提示不声称文件已落盘 |

## 开发浏览器走查

通过 CUA 真实浏览器点击、填写和只读 DOM 观察执行，不以源码检查代替操作。以下为本轮范围，原63项历史结果另见原报告，不宣称全部63项重新执行。

| 路径 | 实际检查与结果 |
| --- | --- |
| 首页和主导航 | 首页、数据中心、三类任务、模板中心、API Keys 均可进入；首页截图无异常遮罩或空白 |
| 12个任务创建入口 | 文档/对话/时序 × 合成/质检/增强/定向扩增全部打开，标题与提交入口正确，均能返回 |
| 文档扩增 | 输入 F3454CDA3969，首组及新增第二组均为 Doubao；下拉仅 Doubao；主模型正确；删除尚未保存的第二组可用 |
| 对话增强 | F232BC2AC51C 98条，未操作数量框时已为98；输入20，改描述及切换任务依据再返回仍为20 |
| 时序发布模板 | COLDTPL-REEFER-MODEL-DEMO-V2 显示已发布、模板ID、版本V1；顺序点击五步，内容及只读状态正常 |
| 时序草稿详情 | COLDDRAFT-20260903-A81C2F 显示草稿已保存、草稿ID，未误显示发布身份 |
| 任务详情 | QA-V5-0916-抽检的基础信息、质检报告、配置快照、运行日志、Badcase页签均可查看 |
| 报告 | 75条规则的汇总、六状态列、抽样范围及新说明可见，下载出现“已开始下载质检报告” |
| 日志 | 下载日志出现“已开始下载日志” |
| 数据集与版本 | QA-V5-0916-输出 → A718C883132B；版本基础信息、图像与标注切换及逐级返回正常 |
| 版本下载 | 更多→下载出现“已开始下载示例 ZIP” |

自测发现并修正了“模板详情参数也可能承载草稿”的边界，随后复测草稿ID正确。发布草稿入口本来会转入配置表单，因此仍执行提交校验。

## 自动化与范围检查

- `npm test`：12组通过，包括新增图像模型约束、98/1/0数量边界、合法值保留测试。
- 下载ZIP回归测试补充真实DOM挂载生命周期模拟，原始文件及报告追加内容检查通过。
- `npm run build`：通过；已有大包体积提示保留。
- `node scripts/verify-v5-scope.mjs`：111个原文件全部保留；58个原来源模块逐字节一致；6个页面Form.Item字段清单不变。仅放行明确授权的下载DOM修复与模板身份文案，不扩大通用豁免。
- 独立对V4基线111文件做SHA256核对：0变化。

## 限制与后续验收

浏览器已确认下载触发反馈；反馈本身不能证明文件已落盘。真实模型调用、真实凭据写入与永久删除未执行。控制台存在既有 Ant Design API弃用及布局配置警告，此次没有做组件库升级。

独立测试工程师使用独立浏览器会话复验，最终结论以《V5缺陷修复独立验收报告-2026-09-16.md》为准。历史失败清单保留，不抹去原始缺陷记录。
