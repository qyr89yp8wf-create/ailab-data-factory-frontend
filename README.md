# 数据合成工具用户端 v5

独立的用户端纯前端原型，包含数据中心、任务中心、模板中心和个人 API Keys 管理。

双击 `start_user.cmd` 启动，访问 <http://127.0.0.1:8769/>。


## V5 视觉重构

由 V4 独立复制，使用 8769 端口。业务数据契约、模型配置、执行逻辑和 API 模块保持原样。
详情改为全宽工作区；任务依据移到同页页签，原有规则、报告与操作完整保留。
独立端口对应独立浏览器存储；不会自动读取或覆盖 V4 在 8768 下保存的浏览器草稿。
详见 `docs/V5交付与回归记录.md`。
